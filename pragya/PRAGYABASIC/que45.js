//Pre-increment (++x): Increments x first, then returns the updated value.
let x = 5, y = ++x;// x = 6, y = 6
console.log(y);

//Post-increment (x++): Returns x first, then increments it.
let a = 5, b = a++; // a = 6, b = 5
console.log(b);